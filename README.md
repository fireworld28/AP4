# AP4
Pour déploié le site localement, il faut PHPStorm et mysql
donc il faut importer la base de données nommée ap4 puis utiliser l'interpreteur php données pour utiliser
pour déploie localement le site web 